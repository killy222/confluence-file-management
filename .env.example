# Phase 4 Docker Compose — copy to .env and set as needed.
# Postgres (backend connects via DATABASE_URL derived from these)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=agents

# Optional: Confluence / NotebookLM (for backend-triggered runs and app service)
# CONFLUENCE_URL=https://wiki.viscomp.net
# CONFLUENCE_USER=a.zdravkov
# CONFLUENCE_PASSWORD=kGXFz4
# NOTEBOOKLM_NOTEBOOK_NAME=Phonix Sales
# NOTEBOOKLM_AUTH_DIR=$HOME/.notebooklm
